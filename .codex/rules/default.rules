# Codex command policy examples. Review before enabling in a real project.

prefix_rule(
    pattern = ["node", "scripts/agent/check.mjs"],
    decision = "allow",
    justification = "Read-only agent harness quality checks.",
)
