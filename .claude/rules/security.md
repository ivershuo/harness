# Security Rules

- Never commit secrets, credentials, private keys, or customer data.
- Inspect untrusted install scripts before running them.
- Changes to auth, authorization, logging, data access, external calls, or
  dependencies require security review.
- Use `docs/SECURITY.md` as the durable security checklist.
